# Database
DATABASE_URL=sqlite.db

# Auth — HMAC signing key for nonces + session cookies (min 16 chars)
NONCE_SECRET=

# Alien / World Developer Portal
APP_ID=app_staging_
NEXT_PUBLIC_APP_ID=app_staging_
DEV_PORTAL_API_KEY=

# Payments — MiniKit Pay
RECIPIENT_ADDRESS=0x

# Settlement — Admin Access
# Comma-separated list of alien_subject (wallet addresses) allowed to settle predictions
ADMIN_ALIEN_SUBJECTS=0x

# Database — PostgreSQL connection string
DATABASE_URL=postgresql://user:password@localhost:5432/signal_market

# Alien Platform — App credentials
ALIEN_APP_ID=
ALIEN_API_KEY=
ALIEN_JWKS_URL=https://sso.alien-api.com/oauth/jwks

# Alien Payments — Webhook verification
ALIEN_WEBHOOK_SECRET=

# Demo Mode (no real funds)
DEMO_MODE=true
NEXT_PUBLIC_DEMO_MODE=true

# Judge Mode (dev-only demo helpers)
JUDGE_MODE=false
NEXT_PUBLIC_JUDGE_MODE=false

# Settlement — Admin Access
# Comma-separated list of alien IDs allowed to settle predictions
ADMIN_ALIEN_IDS=
